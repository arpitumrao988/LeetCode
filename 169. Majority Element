class Solution {
public:
    int majorityElement(vector<int>& nums) {
       map<int,int>freq;
       int n=nums.size();
       for(auto it:nums)
       {
        freq[it]++;
        if(freq[it]>n/2)
        {
            return it;
        }
       } 
       return -1;
    }
};
